# Python dependencies for claudeshorts.
# Install into a venv (system setuptools may fail to build feedparser's
# legacy sgmllib3k dep):
#   python3 -m venv .venv
#   .venv/bin/pip install --upgrade pip setuptools wheel
#   .venv/bin/pip install -r requirements.txt
# Or install the package itself (same deps): pip install -e .

anthropic>=0.40
feedparser>=6.0
httpx>=0.27
typer>=0.12
pyyaml>=6.0
fastapi>=0.115
uvicorn>=0.30
jinja2>=3.1
python-dotenv>=1.0
psycopg[binary]>=3.2
