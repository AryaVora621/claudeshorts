"""Claude generation: select topics, build slides + per-platform captions.

Implemented in Phase 2. Reads content memory (threads/posts) to dedupe and
write follow-ups instead of repeating prior coverage.
"""
